package com.elinacn.subtrack.ui.theme

import androidx.compose.ui.graphics.Color

val PastelBlue = Color(0xFFAEC6CF)
val PastelMint = Color (0xFFB2E2D2)
val PastelGray = Color(0xFFF8F9FA)

/** Same hue as PastelBlue, dark enough to read as text on a white card (5.41:1). */
val DeepBlue = Color(0xFF46707F)

/** Light theme backdrop. Sits under white cards so they separate without a hard edge. */
val SoftBlueGray = Color(0xFFD5DEE2)

val DarkText = Color(0xFF2D3436)

/** Dark theme backdrop. Sits behind DarkText surfaces so cards stay distinguishable. */
val DarkBackground = Color(0xFF1C2022)
